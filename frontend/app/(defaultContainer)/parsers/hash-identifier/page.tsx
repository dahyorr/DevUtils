import HashIdentifier from "@/components/HashIdentifier";
import { Suspense } from "react";

const HashIdentifierPage = () => {
  return (
    <Suspense>
      <HashIdentifier/>
    </Suspense>
  );
}

export default HashIdentifierPage;
